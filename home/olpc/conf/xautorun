#if [ "x`pgrep xchat`" = "x" ]; then xchat; fi&
if [ "x`pgrep screen`" = "x" ]; then screen -tqA mus -d -m; fi&
bgs $HOME/conf/bg.jpg
mkfontdir $HOME/conf/.fonts
xset fp+ $HOME/conf/.fonts
xset b 
xbindkeys&
xmodmap -e "keysym Escape = Delete"
if [[ -f ~/conf/first-boot ]]
then
	~/conf/first-boot
fi
